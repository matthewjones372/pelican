org.gradle.jvmargs=-Xmx1536m -XX:MaxMetaspaceSize=512m
org.gradle.parallel=true
org.gradle.caching=true
kotlin.code.style=official

# The version every module publishes at. Nothing has been released yet, which
# is what the -SNAPSHOT says; drop it to cut one.
pelicanVersion=0.1.0-SNAPSHOT
